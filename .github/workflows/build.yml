name: CI

on:
  push:
    tags:
      - "*"
  pull_request:
    branches:
      - "*"

jobs:
  build:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: [windows-2019, ubuntu-18.04]
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - uses: ilammy/msvc-dev-cmd@v1
      - name: Build
        shell: bash
        run: |
          cd $GITHUB_WORKSPACE
          if [ "$RUNNER_OS" == "Windows" ]; then
            cl.exe //LD //Fe:always-return-true-in-the-wlan-comm-id-check.dll plugin.c
          else
            gcc plugin.c -shared -o always-return-true-in-the-wlan-comm-id-check.so
          fi
      - name: Prepare to upload
        if: github.event_name == 'push'
        shell: bash
        run: |
          cd $GITHUB_WORKSPACE
          export VERSION=${GITHUB_REF:10}
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          if [ "$RUNNER_OS" == "Windows" ]; then
            7z a vvctre-plugin-always-return-true-in-the-wlan-comm-id-check-$VERSION-${{runner.os}}.7z always-return-true-in-the-wlan-comm-id-check.dll license.txt
          else
            7z a vvctre-plugin-always-return-true-in-the-wlan-comm-id-check-$VERSION-${{runner.os}}.7z always-return-true-in-the-wlan-comm-id-check.so license.txt
          fi
      - name: Upload to GitHub Releases
        if: github.event_name == 'push'
        uses: svenstaro/upload-release-action@v1-release
        with:
          repo_token: ${{secrets.GITHUB_TOKEN}}
          file: vvctre-plugin-always-return-true-in-the-wlan-comm-id-check-${{env.VERSION}}-${{runner.os}}.7z
          asset_name: vvctre-plugin-always-return-true-in-the-wlan-comm-id-check-${{env.VERSION}}-${{runner.os}}.7z
          tag: ${{github.ref}}
          overwrite: true
      - name: Upload artifact
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v1
        with:
          name: vvctre-plugin-always-return-true-in-the-wlan-comm-id-check-${{runner.os}}
          path: .
